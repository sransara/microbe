PROGRAM p
BEGIN

	INT a,b,c,d;
	
	FUNCTION VOID main()
	BEGIN
		a := 20;
		b := 30;
		c := 40;
                d := 50;

		c := c + a*b + (a*b+c)/a + 20 * d;
                c := d*a + (b/c);

                b := a*d;
                b := c;

		WRITE (c);
		WRITE (b);
		WRITE (a);
		WRITE (d);
	
	END
END
